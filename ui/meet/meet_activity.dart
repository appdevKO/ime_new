import 'package:flutter/material.dart';

class MeetActivity extends StatelessWidget {
  const MeetActivity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('聯誼'),
      ),
    );
  }
}
